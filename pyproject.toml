[project]
name = "sta"
version = "1.0.0"
authors = [
  { grupo = "11" },
]
description = "Proyecto Saludtech de los Alpes"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = [
  ".", "src",
]